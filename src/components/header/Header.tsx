import Navbar from "../navbar/Navbar";

// * TYPES
interface Props {}

// * COMPONENT: Header
function Header({}: Props) {
  // @ Output
  return (
    <section>
      <Navbar />
    </section>
  );
}

export default Header;
