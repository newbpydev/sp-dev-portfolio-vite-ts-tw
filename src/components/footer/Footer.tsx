import Navbar from "../navbar/Navbar";

// * TYPES
interface Props {}

// * COMPONENT: Footer
function Footer({}: Props) {
  // @ Output
  return (
    <section>
      <Navbar />
    </section>
  );
}

export default Footer;
